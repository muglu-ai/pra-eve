# AGENTS.md

## Cursor Cloud specific instructions

This repository (`pra-eve`) is currently an empty/skeleton project with only a `README.md`. There are no application services, dependencies, build systems, or test suites configured yet.

### Current state
- **No package manager** configured (no `package.json`, `requirements.txt`, `pyproject.toml`, `go.mod`, etc.)
- **No services** to start
- **No tests** to run
- **No linter** configured
- **No build step** required

### For future agents
Once application code is added to this repository, update this file with:
- How to install dependencies
- How to run the development server(s)
- How to run tests and linting
- Any non-obvious setup caveats
